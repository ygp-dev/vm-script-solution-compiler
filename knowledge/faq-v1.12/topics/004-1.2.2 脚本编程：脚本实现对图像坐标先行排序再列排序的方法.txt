1.2.2 脚本编程：脚本实现对图像坐标先行排序再列排序的方法 脚本实现对图像坐标先行排序再列排序的方法 描述 环境： VM4.0 及以上 现象： 如何使用脚本实现图像点先行后列的排序? 解答 该排序需要使用脚本实现： 第一步: 先根据设置的阈值对点集进行按行聚类； 第二步:将二维数组先按行排序； 第三步:遍历每一行进行按列坐标排序； 示例代码如下： C # using System ; using System . Text ; using System . Windows . Forms ; using Script . Methods ; using System . Collections . Generic ; public struct Point { public float x ; public float y ; public float disx ; public float disy ; public Point ( float _x , float _y , float _disx , float _disy ) { this . x = _x ; this . y = _y ; this . disx = _disx ; this . disy = _disy ; } } public struct PointList { public float avgY ; public List Point > pointList ; public PointList ( float _avgY , Point _point ) { this . avgY = _avgY ; this . pointList = new List Point >(); pointList . Add ( _point ); } } public partial class UserScript : ScriptMethods , IProcessMethods { List PointList > pointList = new List PointList >(); /// /// Initialize the field's value when compiling /// 预编译时变量初始化 /// public void Init () { //变量初始化，其余变量可在该函数中添加 } //冒泡排序 public static void BubbleSort ( ref List PointList > pointlist ) { for ( int i = 0 ; i pointlist . Count - 1 ; i ++) { for ( int j = 0 ; j pointlist . Count - 1 - i ; j ++) { if ( pointlist [ j ]. avgY > pointlist [ j + 1 ]. avgY ) { var temp = pointlist [ j ]; pointlist [ j ] = pointlist [ j + 1 ]; pointlist [ j + 1 ] = temp ; } } } } /// /// Enter the process function when running code once /// 流程执行一次进入Process函数 /// /// public bool Process () { //////////获取输入数据////////// int count1 = 0 ; int count2 = 0 ; float dis = 0 ; GetFloatValue ( "dis" , ref dis ); GetIntValue ( "count1" , ref count1 ); GetIntValue ( "count2" , ref count2 ); float [] pointX1 = new float [ count1 ]; float [] pointY1 = new float [ count1 ]; float [] disX1 = new float [ count1 ]; float [] disY1 = new float [ count1 ]; float [] pointX2 = new float [ count2 ]; float [] pointY2 = new float [ count2 ]; float [] disX2 = new float [ count2 ]; float [] disY2 = new float [ count2 ]; GetFloatArrayValue ( "pointX1" , ref pointX1 , out count1 ); GetFloatArrayValue ( "pointY1" , ref pointY1 , out count1 ); GetFloatArrayValue ( "disX1" , ref disX1 , out count1 ); GetFloatArrayValue ( "disY1" , ref disY1 , out count1 ); GetFloatArrayValue ( "pointX2" , ref pointX2 , out count2 ); GetFloatArrayValue ( "pointY2" , ref pointY2 , out count2 ); GetFloatArrayValue ( "disX2" , ref disX2 , out count2 ); GetFloatArrayValue ( "disY2" , ref disY2 , out count2 ); ///////////数据整合//////////// pointList . Clear (); for ( int i = 0 ; i count1 ; i ++) { int flag = 0 ; foreach ( var item in pointList ) { if ( Math . Abs ( pointY1 [ i ]- item . avgY ) dis ) { item . pointList . Add ( new Point ( pointX1 [ i ], pointY1 [ i ], disX1 [ i ], disY1 [ i ])); flag = 1 ; } } if ( flag == 0 ) { pointList . Add ( new PointList ( pointY1 [ i ], new Point ( pointX1 [ i ], pointY1 [ i ], disX1 [ i ], disY1 [ i ]))); } } for ( int i = 0 ; i count2 ; i ++) { int flag = 0 ; foreach ( var item in pointList ) { if ( Math . Abs ( pointY2 [ i ]- item . avgY ) dis ) { item . pointList . Add ( new Point ( pointX2 [ i ], pointY2 [ i ], disX2 [ i ], disY2 [ i ])); flag = 1 ; } } if ( flag == 0 ) { pointList . Add ( new PointList ( pointY2 [ i ], new Point ( pointX2 [ i ], pointY2 [ i ], disX2 [ i ], disY2 [ i ]))); } } /////////////排序////////// //pointlist按y排序 BubbleSort ( ref pointList ); //pointlist每个list按x排序 foreach ( var item in pointList ) { item . pointList . Sort (( item1 , item2 )=> { return item1 . x > item2 . x ? 1 :- 1 ; }); } ////////////输出//////////// int count = 0 ; for ( int i = 0 ; i pointList . Count ; i ++) { for ( int j = 0 ; j pointList [ i ]. pointList . Count ; j ++) { SetFloatValueByIndex ( "sort_x" , pointList [ i ]. pointList [ j ]. x , count , count1 + count2 ); SetFloatValueByIndex ( "sort_y" , pointList [ i ]. pointList [ j ]. y , count , count1 + count2 ); SetFloatValueByIndex ( "sort_disX" , pointList [ i ]. pointList [ j ]. disx , count , count1 + count2 ); SetFloatValueByIndex ( "sort_disY" , pointList [ i ]. pointList [ j ]. disy , count , count1 + count2 ); count ++; } } return true ; } } 问题根因 不熟悉排序编程。