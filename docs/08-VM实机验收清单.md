# VM 4.4 实机验收清单

Codex 不启动或操作 VisionMaster。以下步骤由用户在可观察的 VM 4.4 界面中完成；用户已反馈全部通过，`vmRuntimeValidated` 已更新为 `true`。

## 已解决的问题

- 已读取用户在 VM 4.4 中保存的非空 DynamicIO 样本，样本 SHA256 为 `7A1C7D53B53109C96CBE3C94D1B1D5C60D823A5B76717493E5219259D6C08927`。
- 已按样本实现 ShellModule 和 PyShellModule 的 `Input`、`Output`、`DynamicInData`、`DynamicOutData` 二进制参数结构。
- 已移除错误的 C# `Input`/`Output`/`dynamic` 示例。官方反编译源码确认 ShellModule 使用生成的强类型属性，确定性生成器输出 `Sum = A + B` 一类代码。
- 用户已确认旧样本中的 C# `A`、`B`、`Sum` 端口可见，Python 编译 0 Errors，全局脚本正常编译；由于这些名称来自用户先前在平台创建的样本，它们不单独作为“编译器新增端口”的验收证据。
- 用户已确认第五版候选从空白载体生成的输入端口可见，但 VM 界面未显示 Requirement 默认值。
- 用户在 VM 4.4 中手工写入并保存的 C# float 初值 `1.25/2.5` 与 Python int 初值 `11/13`，均可从 `VmServer.xml/ModuleSubscribe` 原样读回；另存后的关系保持不变。该项确认“初值持久化映射”，不等同于脚本执行结果已经验证。
- 自动生成的 Python 在读取输入为 `None` 时使用 Requirement 默认值，避免 `int(None)`；这保证脚本级回退，但不伪装成 VM UI 初值。
- 2026-07-15，用户对最终候选反馈“3个脚本都正常”。该反馈确认 GlobalScript、C# ShellModule、PyShellModule 在用户 VM 环境中的脚本验收通过。
- 2026-07-15，用户进一步确认四个默认值均在 UI 可见，保存、关闭并重新打开后仍正常。最终候选的脚本、端口、默认值和保存重开持久化验收全部通过。

映射细节见 [09-DynamicIO实机映射.md](09-DynamicIO实机映射.md)。

## 本次候选方案

打开：

```text
outputs/final-user-validation/20260715080423426-runtime-generated-validation/result.sol
```

候选 SHA256：

```text
239D868BA8B84917050795094627ED0AB284FA9993EA7EED58C68B8E4CE557E1
```

编译器侧验证结果：

- 内置解析器和 inspect 均成功，warnings 为 0。
- 流程 `流程1` 含 `CSharp端口验证` 和 `Python端口验证`，两者前后执行连接均为 0。
- C# 源码由 operations 生成 `Sum = (A + B)`，不使用 `Input`、`Output` 或 `dynamic`。
- Python 源码由同一表达式 IR 生成，并对空输入使用 101/202 回退。
- 解析结果确认新的 C#、Python 输入端口；六个端口名全部由编译器写入。
- 解析器从生成结果读回四个默认值：C# 为 1.25/2.5，Python 为 101/202。
- SOL ZIP 条目使用 VM 4.4 兼容的反斜杠名称。

## 用户验收步骤（已全部通过）

1. 用 VisionMaster 4.4 打开候选 `result.sol`。
2. 确认流程画布存在互不连接的 `CSharp端口验证` 和 `Python端口验证`。
3. 打开 `CSharp端口验证`，确认 `A`、`B`、`Sum` 可见，编译并在手工输入后验证求和；不要把 XML 中存在的值关系当作 UI 默认值已通过。
4. 打开 `Python端口验证`，确认 `A`、`B`、`Sum` 可见；编译预期 0 Errors。
5. 输入为空时，使用带脚本级默认值回退的新生成样本验证 Python；手工赋值时再验证端口实际读写。
6. 三类脚本正常已由用户确认，无需重复脚本功能验收。
7. 另存为同目录下的 `result-resaved.sol`，不要覆盖候选文件。
8. 关闭并重新打开 `result-resaved.sol`，确认两个模块、新端口和源码仍存在，再运行一次 Python 求和。

## 通过条件与结果

全部步骤已由用户在 VisionMaster 4.4 中验证通过，manifest 的 `vmRuntimeValidated` 已更新为 `true`。

C# ShellModule 强类型端口属性已由项目内官方反编译源码确认，生成器不再使用 `Input`/`Output`/`dynamic`。三类生成脚本、UI 默认值以及另存、关闭、重开后的持久化均已获用户确认。
