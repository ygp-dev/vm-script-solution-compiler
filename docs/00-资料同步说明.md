# 资料同步说明

本项目已从前期 `vm-script-skill` 原型同步所有实现所需资料，后续开发以本项目内容为唯一来源，不再回读旧项目。

已同步：

- VM 4.4 技术参考与三类脚本契约。
- VM 环境变量检测和 GlobalScript 路径检测原型。
- ShellModule/PyShellModule 解包模板；正式项目内无 `.sol` 文件。
- C#、Python 用户脚本源码模板。
- VM 官方全局脚本默认工程和中文 4.x 示例。
- VMSolutionParser CLI 与运行依赖。
- 全局脚本检查和临时 SOL 物化原型脚本。
- 资源版本、结构信息、SHA-256 和验证状态 manifest。

未同步：

- 两份原始 PDF：知识已整理进 `04-VM4.4技术参考.md`，避免运行时重复读取。
- 开发用 `脚本方案.sol`：只同步了解包后的 `SolutionFile` 结构。
- 旧 skill 的 `SKILL.md`、Agent UI 元数据和重复需求文档。

资源状态中的 `vmRuntimeValidated=true` 表示 2026-07-15 用户已在 VisionMaster 4.4 中确认三类脚本正常、四个默认值可见，并且保存、关闭、重开后仍正常。
